import { describe, expect, it } from 'vitest';
import { physicalCablePresentation } from './physicalCablePresentation';
import type { TopologyProjectionDocument } from './types';
const ref=(entity_type:string,entity_id:string)=>({ref_type:'CANONICAL_FACT',entity_type,entity_id});
describe('physicalCablePresentation',()=>{it('collapses only an unambiguous two-ended cable without mutating projection',()=>{const d:TopologyProjectionDocument={schema_version:'1.0',layer:'L1',detail_level:'PHYSICAL_OBJECT',gaps:[],warnings:[],nodes:[{id:'a',kind:'PHYSICAL_OBJECT',label:'A',source_refs:[ref('ConnectionPoint','a')],attributes:{}},{id:'c',kind:'PHYSICAL_OBJECT',label:'c',source_refs:[ref('PhysicalObject','c'),ref('ConnectionPoint','c1'),ref('ConnectionPoint','c2')],attributes:{class:'cable',connection_point_count:2}},{id:'b',kind:'PHYSICAL_OBJECT',label:'B',source_refs:[ref('ConnectionPoint','b')],attributes:{}}],edges:[{id:'ac',from_node_id:'a',to_node_id:'c',kind:'L1_PHYSICAL_LINK',aggregate:true,source_refs:[],attributes:{endpoint_pairs:[{from_connection_point_id:'a',from_member_index:1,to_connection_point_id:'c1',to_member_index:1,connection_id:'x',connection_member_id:'x1'}]}},{id:'cb',from_node_id:'c',to_node_id:'b',kind:'L1_PHYSICAL_LINK',aggregate:true,source_refs:[],attributes:{endpoint_pairs:[{from_connection_point_id:'c2',from_member_index:1,to_connection_point_id:'b',to_member_index:1,connection_id:'y',connection_member_id:'y1'}]}}]};const before=structuredClone(d);const p=physicalCablePresentation(d);expect(p.nodes.map(n=>n.id)).toEqual(['a','b']);expect(p.edges).toEqual([]);expect(p.cables[0].source).toBe('a');expect(p.cables[0].target).toBe('b');expect(d).toEqual(before);});});
