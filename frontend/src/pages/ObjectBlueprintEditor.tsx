import { useMemo, useState } from 'react';
import { BlueprintPreviewViewport } from '../components/BlueprintPreview';
import { createBlueprintRequest, generateBlueprint, generatedGroupKeys, type BlueprintEditorState, type EndpointGroup } from '../blueprints/editorModel';
import type { BlueprintAnchorSide, BlueprintSlotKind } from '../topology/objectBlueprintTypes';

const sides: BlueprintAnchorSide[] = ['LEFT', 'RIGHT', 'TOP', 'BOTTOM'];
const kinds: BlueprintSlotKind[] = ['CONNECTION_POINT', 'NETWORK_PORT'];
const sideLabel: Record<BlueprintAnchorSide, string> = { LEFT: 'Слева', RIGHT: 'Справа', TOP: 'Сверху', BOTTOM: 'Снизу' };
const kindLabel: Record<BlueprintSlotKind, string> = { CONNECTION_POINT: 'Точка подключения', NETWORK_PORT: 'Сетевой порт' };
let nextId = 2;
const newGroup = (): EndpointGroup => ({ id: `group-${nextId++}`, keyPrefix: '', displayPrefix: '', kind: 'CONNECTION_POINT', side: 'LEFT', count: 1, startingNumber: 1, placementOffset: 0, placementSpan: 1 });
const colorValue = (value: string) => /^#[0-9A-Fa-f]{6}$/.test(value) ? value : '#28565a';
const groupLabel = (group: EndpointGroup, index?: number) => group.displayPrefix || group.keyPrefix || `Группа ${(index ?? 0) + 1}`;
export const newBlueprintEditorState = (): BlueprintEditorState => ({ name: '', defaultClass: '', width: 120, height: 60, fillColor: '#28565a', groups: [{ id: 'group-1', keyPrefix: 'A', displayPrefix: 'A', kind: 'CONNECTION_POINT', side: 'LEFT', count: 1, startingNumber: 1, placementOffset: 0, placementSpan: 1 }], pairs: [] });

export function ObjectBlueprintEditor({ initialState, title, description, saveLabel, versionNotice, onSave }: { initialState: BlueprintEditorState; title: string; description: string; saveLabel: string; versionNotice?: string; onSave: (state: BlueprintEditorState) => Promise<void> }) {
  const [editor, setEditor] = useState(initialState);
  const [saveError, setSaveError] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);
  const [zoom, setZoom] = useState(1);
  const generated = useMemo(() => generateBlueprint(editor), [editor]);
  const update = (id: string, field: keyof EndpointGroup, value: string | number) => setEditor(s => ({ ...s, groups: s.groups.map(g => g.id === id ? { ...g, [field]: value } : g) }));
  const save = async () => { const result = createBlueprintRequest(editor); if (!result.request) { setSaveError(result.errors.join(' ')); return; } setSaveError(null); setSaving(true); try { await onSave(editor); } catch (reason) { setSaveError(reason instanceof Error ? reason.message : 'Не удалось сохранить шаблон.'); } finally { setSaving(false); } };
  return <><header className="catalog-page__header"><div><span className="shell-nav__group-label">Визуальный редактор</span><h1>{title}</h1><p>{description}</p>{versionNotice && <p className="blueprint-editor__notice">{versionNotice}</p>}</div></header><div className="blueprint-editor-layout"><section className="blueprint-editor-controls"><label>Название шаблона<input aria-label="Название шаблона" value={editor.name} onChange={e => setEditor({ ...editor, name: e.target.value })}/></label><label>Тип объекта<input aria-label="Тип объекта" value={editor.defaultClass} onChange={e => setEditor({ ...editor, defaultClass: e.target.value })}/><span className="blueprint-editor__hint">Необязательная классификация, например switch или patch_panel.</span></label><div className="blueprint-editor-controls__row"><label>Ширина<input aria-label="Ширина" type="number" value={editor.width} onChange={e => setEditor({ ...editor, width: Number(e.target.value) })}/></label><label>Высота<input aria-label="Высота" type="number" value={editor.height} onChange={e => setEditor({ ...editor, height: Number(e.target.value) })}/></label></div><p className="blueprint-editor__hint">Это схематическая форма шаблона; размер на карте определяет renderer.</p><div className="blueprint-editor-color"><label>Цвет<input aria-label="Выбор цвета" type="color" value={colorValue(editor.fillColor)} onChange={e => setEditor({ ...editor, fillColor: e.target.value })}/></label><label>Точный цвет (hex)<input aria-label="Цвет (hex)" value={editor.fillColor} onChange={e => setEditor({ ...editor, fillColor: e.target.value })}/></label></div><h2>Группы портов</h2><p className="blueprint-editor__hint">Группа создаёт последовательность портов с общей маркировкой и расположением на схеме.</p>{editor.groups.map((g, i) => <fieldset className="endpoint-group" key={g.id}><legend>Группа {i + 1}: {groupLabel(g, i)}</legend><p className="endpoint-group__summary">{generatedGroupKeys(g).join(', ')} · {kindLabel[g.kind]} · {sideLabel[g.side]}</p><label>Префикс ключа<input aria-label={`Префикс ключа ${i + 1}`} value={g.keyPrefix} onChange={e => update(g.id, 'keyPrefix', e.target.value)}/><span className="blueprint-editor__hint">Стабильный идентификатор порта.</span></label><label>Префикс отображаемого имени<input aria-label={`Префикс отображаемого имени ${i + 1}`} value={g.displayPrefix} onChange={e => update(g.id, 'displayPrefix', e.target.value)}/></label><div className="blueprint-editor-controls__row"><label>Тип порта<select aria-label={`Тип порта ${i + 1}`} value={g.kind} onChange={e => update(g.id, 'kind', e.target.value as BlueprintSlotKind)}>{kinds.map(x => <option key={x} value={x}>{kindLabel[x]}</option>)}</select></label><label>Сторона схемы<select aria-label={`Сторона схемы ${i + 1}`} value={g.side} onChange={e => update(g.id, 'side', e.target.value as BlueprintAnchorSide)}>{sides.map(x => <option key={x} value={x}>{sideLabel[x]}</option>)}</select></label></div><div className="blueprint-editor-controls__row"><label>Количество портов<input aria-label={`Количество портов ${i + 1}`} type="number" value={g.count} onChange={e => update(g.id, 'count', Number(e.target.value))}/></label><label>Начать с номера<input aria-label={`Начать с номера ${i + 1}`} type="number" value={g.startingNumber} onChange={e => update(g.id, 'startingNumber', Number(e.target.value))}/></label></div><div className="blueprint-editor-controls__row"><label>Начало диапазона (0–1)<input aria-label={`Начало диапазона ${i + 1}`} type="number" min="0" max="1" step="0.01" value={g.placementOffset} onChange={e => update(g.id, 'placementOffset', Number(e.target.value))}/></label><label>Длина диапазона (0–1)<input aria-label={`Длина диапазона ${i + 1}`} type="number" min="0.01" max="1" step="0.01" value={g.placementSpan} onChange={e => update(g.id, 'placementSpan', Number(e.target.value))}/></label></div><span className="blueprint-editor__hint">Порты равномерно располагаются внутри этого диапазона выбранной стороны.</span><button type="button" onClick={() => setEditor(s => ({ ...s, groups: s.groups.filter(x => x.id !== g.id), pairs: s.pairs.filter(p => p.leftGroupId !== g.id && p.rightGroupId !== g.id) }))}>Удалить группу</button></fieldset>)}<button type="button" className="secondary-action" onClick={() => setEditor(s => ({ ...s, groups: [...s.groups, newGroup()] }))}>Добавить группу портов</button><h2>Внутренние пары портов</h2><p className="blueprint-editor__hint">Правило соединяет порт N одной группы с портом N другой группы. Например A01 ↔ B01, A02 ↔ B02.</p>{editor.pairs.map((p, i) => <div className="blueprint-pair" key={`${p.leftGroupId}-${p.rightGroupId}-${i}`}><label><span className="sr-only">Первая группа правила {i + 1}</span><select aria-label={`Первая группа правила ${i + 1}`} value={p.leftGroupId} onChange={e => setEditor(s => ({ ...s, pairs: s.pairs.map((x, n) => n === i ? { ...x, leftGroupId: e.target.value } : x) }))}>{editor.groups.map((g, index) => <option key={g.id} value={g.id}>{groupLabel(g, index)}</option>)}</select></label><span>↔</span><label><span className="sr-only">Вторая группа правила {i + 1}</span><select aria-label={`Вторая группа правила ${i + 1}`} value={p.rightGroupId} onChange={e => setEditor(s => ({ ...s, pairs: s.pairs.map((x, n) => n === i ? { ...x, rightGroupId: e.target.value } : x) }))}>{editor.groups.map((g, index) => <option key={g.id} value={g.id}>{groupLabel(g, index)}</option>)}</select></label><button type="button" aria-label={`Удалить правило ${i + 1}`} onClick={() => setEditor(s => ({ ...s, pairs: s.pairs.filter((_, n) => n !== i) }))}>Удалить</button></div>)}<button type="button" className="secondary-action" disabled={editor.groups.length < 2} onClick={() => setEditor(s => s.groups.length < 2 ? s : { ...s, pairs: [...s.pairs, { leftGroupId: s.groups[0].id, rightGroupId: s.groups[1].id }] })}>Добавить правило пар по номеру</button>{(saveError || generated.errors.length > 0) && <p role="alert" className="blueprint-editor__error">{saveError || generated.errors.join(' ')}</p>}<button type="button" className="primary-action" disabled={saving} onClick={() => void save()}>{saving ? 'Сохраняем…' : saveLabel}</button></section><section className="blueprint-editor-preview"><div className="blueprint-editor-preview__heading"><h2>Предпросмотр схемы</h2><div><button type="button" aria-label="Уменьшить масштаб" onClick={() => setZoom(v => Math.max(.25, v - .25))}>−</button><button type="button" aria-label="Сбросить масштаб" onClick={() => setZoom(1)}>{Math.round(zoom * 100)}%</button><button type="button" aria-label="Увеличить масштаб" onClick={() => setZoom(v => Math.min(2, v + .25))}>+</button><button type="button" onClick={() => setZoom(1)}>Вписать</button></div></div><BlueprintPreviewViewport body={{ kind: 'RECTANGLE', width: Math.max(editor.width, 1), height: Math.max(editor.height, 1), fill_color: editor.fillColor || undefined }} slots={generated.slots} internalLinks={generated.internalLinks} scale={zoom}/><p>Портов: {generated.slots.length} · внутренних связей: {generated.internalLinks.length}</p></section></div></>;
}
